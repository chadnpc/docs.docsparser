---
title: 'Authentication'
---

Include your DocsParser Secret Key, found in your dashboard, as a Bearer token in the `Authorization` header:

```
Authorization: Bearer {your-docsparser-secret-key}
```